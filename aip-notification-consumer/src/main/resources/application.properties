spring.application.name=aip-notification-consumer
server.port=8083

broker.base-url=${BROKER_URL:http://localhost:8080}

consumer.id=notification-consumer
consumer.name=Notification Consumer
consumer.topic=orders
consumer.poll-max=10

logging.level.com.notification=INFO
