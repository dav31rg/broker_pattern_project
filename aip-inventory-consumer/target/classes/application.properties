spring.application.name=aip-inventory-consumer
server.port=8082

broker.base-url=${BROKER_URL:http://localhost:8080}

consumer.id=inventory-consumer
consumer.name=Inventory Consumer
consumer.topic=orders
consumer.poll-max=10

logging.level.com.inventory=INFO
